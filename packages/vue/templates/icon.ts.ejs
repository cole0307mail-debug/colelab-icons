// This icon file is generated automatically.
import { defineComponent, h } from 'vue';
import <%= svgIdentifier %>Svg from '@colelab/icons-svg/es/asn/<%= svgIdentifier %>';
import SensoroIcon from '../components/SensoroIcon';

export default defineComponent({
  name: '<%= svgIdentifier %>',
  inheritAttrs: false,
  setup(_, { attrs, slots }) {
    return () => h(SensoroIcon as any, { ...attrs, icon: <%= svgIdentifier %>Svg }, slots);
  },
});
