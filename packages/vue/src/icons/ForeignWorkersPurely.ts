// This icon file is generated automatically.
import { defineComponent, h } from 'vue';
import ForeignWorkersPurelySvg from '@colelab/icons-svg/es/asn/ForeignWorkersPurely';
import SensoroIcon from '../components/SensoroIcon';

export default defineComponent({
  name: 'ForeignWorkersPurely',
  inheritAttrs: false,
  setup(_, { attrs, slots }) {
    return () => h(SensoroIcon as any, { ...attrs, icon: ForeignWorkersPurelySvg }, slots);
  },
});
